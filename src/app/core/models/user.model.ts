// TODO: this will be used to define models in our app (not from APIs)
// this will be used as models for the state object
// example:
export interface UserSavedPaymentMethod {
    paymentMethod: string;
}
